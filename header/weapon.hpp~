#ifndef __WEAPON_HPP__
#define __WEAPON_HPP__

#include "loot.hpp"

class weapon: public Loot
{
private:
    int value;
public:
    weapon(int value) : value(value) {};

    virtual void use(BasicUnit* target) {
	target->SetWeapon(this);
    }

    int GetValue(){
	return value;
    }
};

#endif
